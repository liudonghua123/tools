# Perl Basic: Syntax & Output
# Single line comments start with #

print "Hello Perl!\n";

# Using 'say' (Perl 5.10+, requires 'use 5.010')
# Note: WebPerl might need explicit newline in print
print "Executing in browser.\n";

$x = 10;
if ($x > 5) {
    print "x is large.\n";
}
