// Rust (Rune) Basic: Variables

pub fn main() {
    // Rune is dynamically typed but uses Rust-like syntax
    let name = "Alice";
    let mut count = 0;
    
    count = count + 1;
    
    println!("Name: {}, Count: {}", name, count);
}
